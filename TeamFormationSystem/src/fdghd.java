import java.util.Random;

public class fdghd {

	public static void main(String[] args) {
		
	
	Random uniqueIdChooser = new Random();
	
	int[] dsfd = new int[]{3,4,5,6};
	
	int randomUniqueId = uniqueIdChooser.nextInt(dsfd.length);
	System.out.println(randomUniqueId);
	
	randomUniqueId = uniqueIdChooser.nextInt(dsfd.length);
	System.out.println(randomUniqueId);
}
}